# RYKNSH Brain Configuration
# bootstrap.sh が参照する設定ファイル
# curl install 時にオーバーライドしたい場合は BRAIN_REPO 環境変数を使う
# フォーク利用者は BRAIN_REPO を自分のリポジトリ URL に変更してください

BRAIN_REPO="https://github.com/RYKNSH/ryknsh-brain.git"
BRAIN_INSTALL_DIR="${HOME}/.claude"
